const courseCards = document.getElementsByClassName("c-card");

// console.log(typeof courseCards);

Object.entries(courseCards).forEach((element) => {
  element.addEventListener("mouseover", () => {
    element.style;
  });
});
